<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".taskapp.TaskApp"
            android:theme="@style/Theme.TaskApp"
            android:exported="false" />
        <activity
            android:name=".ImcApp.ImcResultActivity"
            android:exported="false" />
        <activity
            android:name=".ImcApp.ImcAppActivity"
            android:exported="false" />
        <activity
            android:name=".MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".firtsapp.ResultActivity"
            android:exported="false" />
        <activity
            android:name=".firtsapp.MyFirtsAppActyvity"
            android:exported="true" />
    </application>

</manifest>